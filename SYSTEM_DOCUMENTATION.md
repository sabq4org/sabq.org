# 📚 وثائق نظام سبق الذكية الشاملة
## Sabq Smart News Platform - Complete System Documentation

> **تاريخ التحديث:** نوفمبر 2025
> **الإصدار:** 2.0

---

# 🏗️ نظرة عامة على النظام

سبق الذكية هي منصة إخبارية ثلاثية اللغات (عربي، إنجليزي، أردو) مدعومة بالذكاء الاصطناعي، مصممة لإحداث ثورة في استهلاك الأخبار. تقدم تلخيصًا ذكيًا بالذكاء الاصطناعي، توصيات مخصصة، إدارة محتوى شاملة، وتوزيع فيروسي على وسائل التواصل الاجتماعي.

### الميزات الرئيسية
- لوحات تحكم ثلاثية اللغات
- إدارة محتوى مستقلة لكل لغة
- روابط ذكية
- تحسين محركات البحث بالذكاء الاصطناعي
- إنشاء محتوى بنقرة واحدة بالذكاء الاصطناعي
- تحليلات تفصيلية للمشاركة الاجتماعية

---

# 🔐 نظام المصادقة والتفويض

## 1. المصادقة (Authentication)

### طرق تسجيل الدخول
| الطريقة | الوصف | API Endpoint |
|---------|-------|--------------|
| البريد الإلكتروني وكلمة المرور | تسجيل دخول تقليدي | `POST /api/login` |
| Google OAuth | تسجيل دخول بحساب جوجل | `GET /api/auth/google` |
| Apple OAuth | تسجيل دخول بحساب آبل | `GET /api/auth/apple` |

### التسجيل
- **إنشاء حساب جديد:** `POST /api/register`
- **التحقق من البريد الإلكتروني:** `POST /api/auth/verify-email`
- **إعادة إرسال رمز التحقق:** `POST /api/auth/resend-verification`
- **استعادة كلمة المرور:** `POST /api/auth/forgot-password`
- **إعادة تعيين كلمة المرور:** `POST /api/auth/reset-password`

### المصادقة الثنائية (2FA)
| الخاصية | الوصف |
|---------|-------|
| التطبيق (TOTP) | استخدام تطبيقات مثل Google Authenticator |
| الرسائل النصية (SMS) | رمز عبر Twilio |
| رموز الاحتياط | 10 رموز للطوارئ |

**API Endpoints:**
- `GET /api/2fa/status` - حالة المصادقة الثنائية
- `GET /api/2fa/setup` - إعداد المصادقة الثنائية
- `POST /api/2fa/enable` - تفعيل المصادقة الثنائية
- `POST /api/2fa/disable` - إلغاء تفعيل المصادقة الثنائية
- `POST /api/2fa/verify` - التحقق من الرمز
- `POST /api/2fa/send-sms` - إرسال رمز SMS
- `POST /api/2fa/verify-sms` - التحقق من رمز SMS
- `POST /api/2fa/backup-codes` - إنشاء رموز احتياطية

## 2. نظام الصلاحيات (RBAC)

### الأدوار المتاحة (8 أدوار)
| الدور | الوصف | المستوى |
|-------|-------|---------|
| `super_admin` | مدير النظام الأعلى | 1 |
| `admin` | مدير عام | 2 |
| `editor_in_chief` | رئيس التحرير | 3 |
| `senior_editor` | محرر أول | 4 |
| `editor` | محرر | 5 |
| `journalist` | صحفي | 6 |
| `contributor` | مساهم | 7 |
| `viewer` | مشاهد | 8 |

### الصلاحيات الرئيسية
```
articles.view, articles.create, articles.edit, articles.delete, articles.publish, articles.feature
users.view, users.create, users.update, users.delete, users.change_role
categories.view, categories.create, categories.edit, categories.delete
comments.view, comments.moderate, comments.delete
media.view, media.upload, media.delete
analytics.view, analytics.export
system.manage_settings, system.manage_roles
```

---

# 📰 نظام إدارة المقالات

## 1. إنشاء وتحرير المقالات

### الخصائص الأساسية
| الحقل | النوع | الوصف |
|-------|------|-------|
| `title` | نص | عنوان المقال |
| `content` | HTML | محتوى المقال (WYSIWYG) |
| `excerpt` | نص | مقتطف المقال |
| `slug` | نص | رابط المقال الفريد |
| `thumbnailUrl` | رابط | صورة المقال الرئيسية |
| `categoryId` | UUID | تصنيف المقال |
| `authorId` | UUID | كاتب المقال |
| `status` | enum | حالة المقال |
| `articleType` | enum | نوع المقال |

### حالات المقال
- `draft` - مسودة
- `pending_review` - قيد المراجعة
- `published` - منشور
- `archived` - مؤرشف
- `scheduled` - مجدول للنشر

### أنواع المقالات
- `news` - خبر
- `opinion` - رأي
- `analysis` - تحليل
- `feature` - تقرير مميز
- `breaking` - خبر عاجل
- `exclusive` - حصري
- `interview` - مقابلة
- `investigation` - تحقيق

### ميزات SEO
| الخاصية | الوصف |
|---------|-------|
| `metaTitle` | عنوان محركات البحث |
| `metaDescription` | وصف محركات البحث |
| `keywords` | الكلمات المفتاحية |
| `socialTitle` | عنوان المشاركة الاجتماعية |
| `socialDescription` | وصف المشاركة الاجتماعية |
| `ogImageUrl` | صورة Open Graph |

### API Endpoints للمقالات
```
GET    /api/admin/articles           - قائمة المقالات
GET    /api/admin/articles/:id       - تفاصيل مقال
POST   /api/admin/articles           - إنشاء مقال
PATCH  /api/admin/articles/:id       - تحديث مقال
DELETE /api/admin/articles/:id       - حذف مقال
POST   /api/admin/articles/:id/publish  - نشر مقال
POST   /api/admin/articles/:id/feature  - تمييز مقال
```

## 2. نظام التصنيفات

### خصائص التصنيف
| الحقل | الوصف |
|-------|-------|
| `name` | اسم التصنيف |
| `slug` | رابط التصنيف |
| `description` | وصف التصنيف |
| `color` | لون التصنيف |
| `icon` | أيقونة التصنيف |
| `parentId` | التصنيف الأب (للتصنيفات الفرعية) |
| `order` | ترتيب العرض |
| `isActive` | حالة التفعيل |

### أنواع التصنيفات
- `standard` - تصنيف عادي
- `dynamic` - تصنيف ديناميكي (الآن)
- `seasonal` - تصنيف موسمي (رمضان، الحج)
- `ai_powered` - تصنيف مدعوم بالذكاء الاصطناعي

### الميزات الخاصة
```javascript
categoryFeatures = {
  realtime: boolean,      // تحديث فوري
  ai_powered: boolean,    // مدعوم بالذكاء الاصطناعي
  trending: boolean,      // الأكثر رواجًا
  breaking_news: boolean, // أخبار عاجلة
  personalized: boolean,  // مخصص للمستخدم
  audio_newsletter: boolean // نشرة صوتية
}
```

---

# 🤖 نظام الذكاء الاصطناعي

## 1. iFox - غرفة أخبار الذكاء الاصطناعي

### لوحة تحكم iFox (`/admin/ifox`)

#### الصفحات الرئيسية
| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Dashboard | `/admin/ifox` | لوحة الإحصائيات الرئيسية |
| Articles | `/admin/ifox/articles` | إدارة المقالات المنشأة بالذكاء الاصطناعي |
| Editor | `/admin/ifox/articles/new` | محرر المقالات الذكي |
| Media | `/admin/ifox/media` | مكتبة الوسائط |
| Schedule | `/admin/ifox/schedule` | جدولة النشر |
| Analytics | `/admin/ifox/analytics` | التحليلات والإحصائيات |
| Categories | `/admin/ifox/categories` | إدارة التصنيفات |
| Settings | `/admin/ifox/settings` | الإعدادات |

### تصنيفات iFox المعتمدة (5 تصنيفات)
| التصنيف | الـ Slug | الوصف |
|---------|----------|-------|
| أخبار الذكاء الاصطناعي | `ai-news` | آخر أخبار AI |
| رؤى وتحليلات | `ai-insights` | تحليلات متعمقة |
| آراء ومقالات | `ai-opinions` | آراء ومقالات رأي |
| أدوات وتطبيقات | `ai-tools` | أدوات AI الجديدة |
| صوت الذكاء الاصطناعي | `ai-voice` | محتوى صوتي |

### نظام إدارة الذكاء الاصطناعي (`/admin/ifox/ai-management`)

#### التبويبات
| التبويب | الملف | الوصف |
|---------|-------|-------|
| التفضيلات | `AiPreferencesTab.tsx` | إعدادات نماذج AI |
| مدير الميزانية | `BudgetManagerTab.tsx` | إدارة تكاليف API |
| قوالب المحتوى | `ContentTemplatesTab.tsx` | قوالب إنشاء المحتوى |
| التقويم التحريري | `EditorialCalendarTab.tsx` | جدولة المحتوى |
| تحليلات الأداء | `PerformanceAnalyticsTab.tsx` | مقاييس الأداء |
| فحوصات الجودة | `QualityChecksTab.tsx` | معايير الجودة |
| رؤى استراتيجية | `StrategyInsightsTab.tsx` | توصيات استراتيجية |
| قواعد سير العمل | `WorkflowRulesTab.tsx` | أتمتة سير العمل |

## 2. مهام الذكاء الاصطناعي (AI Tasks)

### دورة حياة المهمة
```
pending → processing → completed/failed
```

### خصائص المهمة
| الحقل | الوصف |
|-------|-------|
| `taskType` | نوع المهمة (article, summary, seo) |
| `status` | حالة المهمة |
| `priority` | الأولوية (1-10) |
| `scheduledFor` | وقت التنفيذ المجدول |
| `config` | إعدادات المهمة |
| `result` | نتيجة التنفيذ |
| `attempts` | عدد المحاولات |
| `maxAttempts` | الحد الأقصى للمحاولات (3) |

### الوظائف المجدولة
| الوظيفة | الملف | الجدول | الوصف |
|---------|-------|--------|-------|
| AI Tasks Executor | `aiTasksJob.ts` | كل دقيقة | تنفيذ المهام المعلقة |
| AI Tasks Cleanup | `aiTasksCleanup.ts` | كل 5 دقائق | تنظيف المهام العالقة |
| iFox Generator | `ifoxContentGeneratorJob.ts` | كل دقيقة | إنشاء محتوى تلقائي |

### API Endpoints
```
GET    /api/admin/ai-tasks           - قائمة المهام
GET    /api/admin/ai-tasks/:id       - تفاصيل مهمة
POST   /api/admin/ai-tasks           - إنشاء مهمة
PATCH  /api/admin/ai-tasks/:id       - تحديث مهمة
DELETE /api/admin/ai-tasks/:id       - حذف مهمة
POST   /api/admin/ai-tasks/:id/retry - إعادة تنفيذ مهمة
```

## 3. استوديو الصور بالذكاء الاصطناعي (`/admin/ifox/image-studio`)

### النماذج المتاحة
| النموذج | الخدمة | الاستخدام |
|---------|--------|----------|
| Nano Banana Pro | خارجي | صور عالية الجودة |
| NotebookLM (Gemini 3 Pro) | Google | إنفوجرافيك |
| Visual AI | متعدد | صور إبداعية |

### أنماط الإنفوجرافيك (5 أنماط)
| النمط | الوصف |
|-------|-------|
| Minimal | تصميم بسيط ونظيف |
| Corporate | تصميم احترافي للشركات |
| Creative | تصميم إبداعي ملون |
| Data-Driven | تركيز على البيانات والإحصائيات |
| Editorial | تصميم صحفي تحريري |

### خصائص الصورة المنشأة
| الحقل | الوصف |
|-------|-------|
| `prompt` | وصف الصورة |
| `style` | نمط الصورة |
| `orientation` | الاتجاه (portrait/landscape/square) |
| `detailLevel` | مستوى التفاصيل |
| `rtlOptimized` | محسنة للعربية |
| `aiDisclosure` | بيانات الشفافية |
| `storageUrl` | رابط التخزين |

## 4. توليد المقالات الذكي

### الخدمات
| الخدمة | الملف | الوظيفة |
|--------|-------|---------|
| AI Article Generator | `aiArticleGenerator.ts` | إنشاء مقالات كاملة |
| AI Image Generator | `aiImageGenerator.ts` | إنشاء صور للمقالات |
| AI Smart Thumbnail | `aiSmartThumbnailService.ts` | صور مصغرة ذكية |
| Content Analyzer | `contentAnalyzer.ts` | تحليل المحتوى |

### ميزات التوليد
- إنشاء عناوين ذكية
- تصنيف تلقائي
- توليد SEO
- إنشاء ملخصات
- توليد صور مميزة
- تحسين للغة العربية RTL

## 5. محرك التحليل العميق (Omq/Deep Analysis)

### الميزات
| الميزة | الوصف |
|--------|-------|
| Multi-Model Analysis | تحليل بعدة نماذج AI |
| SSE Streaming | بث النتائج في الوقت الفعلي |
| Source Attribution | نسب المصادر |
| Expert Insights | رؤى الخبراء |

### API Endpoints
```
GET  /api/deep-analyses           - قائمة التحليلات
GET  /api/deep-analyses/:id       - تفاصيل تحليل
POST /api/admin/deep-analyses     - إنشاء تحليل
GET  /api/deep-analyses/:id/stream - بث SSE
```

## 6. وكيل الصحفي الذكي (Smart Journalist Agent)

### القدرات
- مساعدة في كتابة الأخبار
- اقتراح عناوين
- تحسين الأسلوب
- التدقيق اللغوي
- إنشاء ملخصات

### API Endpoints
```
POST /api/journalist-agent/assist     - طلب مساعدة
POST /api/journalist-agent/headlines  - اقتراح عناوين
POST /api/journalist-agent/summarize  - تلخيص محتوى
```

---

# 📻 نظام النشرات الصوتية

## 1. نظرة عامة

منظومة بث صوتي شاملة مع تكامل ElevenLabs لتحويل النص إلى كلام عربي.

## 2. قوالب النشرات (8 قوالب)
| القالب | الوصف | التوقيت |
|--------|-------|---------|
| Morning Brief | أبرز أخبار اليوم لبداية يومك | 6:00 صباحاً |
| Evening Digest | ملخص أهم أحداث اليوم | 8:00 مساءً |
| Weekly Roundup | تحليل معمق لأبرز أحداث الأسبوع | الأحد 10:00 صباحاً |
| Breaking News | نشرة الأخبار العاجلة | فوري |
| Sports Update | أخبار الرياضة | حسب الطلب |
| Business News | أخبار الأعمال والاقتصاد | حسب الطلب |
| Tech News | أخبار التقنية | حسب الطلب |
| Custom | نشرة مخصصة | حسب الطلب |

## 3. الأصوات العربية (8 أصوات)

### أصوات ذكورية (4)
| الصوت | الاستخدام | الخصائص |
|-------|----------|---------|
| Ahmad | أخبار رسمية | صوت واثق ومهني |
| Khalid | رياضة | صوت حماسي |
| Youssef | تقنية | صوت شبابي |
| Omar | تحليلات | صوت عميق |

### أصوات أنثوية (4)
| الصوت | الاستخدام | الخصائص |
|-------|----------|---------|
| Fatima | أخبار عامة | صوت واضح ودافئ |
| Layla | ثقافة | صوت راقٍ |
| Nour | صباحي | صوت منعش |
| Sara | مسائي | صوت هادئ |

## 4. إعدادات الصوت
| الإعداد | النطاق | الافتراضي | الوصف |
|---------|--------|----------|-------|
| `playbackSpeed` | 0.5x - 2x | 1x | سرعة التشغيل |
| `stability` | 0 - 1 | 0.5 | استقرار الصوت |
| `similarityBoost` | 0 - 1 | 0.75 | تعزيز التشابه |
| `style` | 0 - 1 | 0.5 | الأسلوب |
| `speakerBoost` | boolean | true | تعزيز المتحدث |

## 5. الصفحات والمكونات

### صفحات الإدارة
| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Dashboard | `/admin/audio-newsletters` | لوحة التحكم الرئيسية |
| Editor | `/admin/audio-newsletters/new` | محرر النشرات |
| Analytics | `/admin/audio-newsletters/analytics` | التحليلات |
| Archive | `/admin/audio-newsletters/archive` | الأرشيف |

### الصفحات العامة
| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Public Page | `/audio-newsletters` | صفحة النشرات العامة |
| Detail | `/audio-newsletters/:id` | تفاصيل نشرة |
| RSS Feed | `/api/audio-newsletters/rss` | خلاصة RSS/Podcast |

## 6. API Endpoints
```
GET    /api/audio-newsletters              - قائمة النشرات
GET    /api/audio-newsletters/:id          - تفاصيل نشرة
POST   /api/admin/audio-newsletters        - إنشاء نشرة
PATCH  /api/admin/audio-newsletters/:id    - تحديث نشرة
DELETE /api/admin/audio-newsletters/:id    - حذف نشرة
POST   /api/admin/audio-newsletters/:id/generate-audio - توليد الصوت
GET    /api/audio-newsletters/:id/stream   - بث الصوت
POST   /api/admin/audio-newsletters/preview-voice - معاينة الصوت
```

## 7. التحليلات
| المقياس | الوصف |
|---------|-------|
| Total Listens | إجمالي الاستماعات |
| Completion Rate | معدل الإكمال |
| Average Duration | متوسط مدة الاستماع |
| Peak Hours | ساعات الذروة |
| Device Distribution | توزيع الأجهزة |

---

# 💬 نظام WhatsApp للنشر التلقائي

## 1. نظرة عامة

نظام نشر تلقائي من WhatsApp إلى المقالات مع تكامل Twilio API.

## 2. سير العمل
```
رسالة WhatsApp → Webhook → معالجة AI → تنظيف المحتوى → إنشاء مقال → نشر
```

## 3. الميزات
| الميزة | الوصف |
|--------|-------|
| Multi-Image Support | دعم صور متعددة |
| AI Content Processing | معالجة المحتوى بالذكاء الاصطناعي |
| Auto-Categorization | تصنيف تلقائي |
| Content Cleaning | تنظيف ذكي للمحتوى |
| Quality Scoring | تقييم جودة المحتوى |
| Auto-Publishing | نشر تلقائي |

## 4. تخزين الصور
- Google Cloud Storage للصور
- تحويل تلقائي للتنسيقات
- ضغط ذكي
- صور مصغرة تلقائية

## 5. API Endpoints
```
POST /api/whatsapp/webhook           - استقبال الرسائل
GET  /api/admin/whatsapp/logs        - سجلات الرسائل
GET  /api/admin/whatsapp/settings    - الإعدادات
PATCH /api/admin/whatsapp/settings   - تحديث الإعدادات
```

## 6. صفحة الإدارة
**المسار:** `/admin/whatsapp`

**الميزات:**
- عرض سجلات الرسائل
- إعدادات المعالجة
- قائمة الأرقام المسموحة
- إحصائيات الاستخدام

---

# 📧 نظام البريد الإلكتروني الذكي

## 1. وكيل البريد الإلكتروني (Email Agent)

### الميزات
| الميزة | الوصف |
|--------|-------|
| Email-to-Article | تحويل البريد إلى مقالات |
| AI Content Analysis | تحليل محتوى بالذكاء الاصطناعي |
| Quality Scoring | تقييم الجودة |
| Auto-Publishing | نشر تلقائي |
| Attachment Handling | معالجة المرفقات |

### سير العمل
```
بريد وارد → تحليل AI → استخراج المحتوى → تقييم الجودة → إنشاء مقال → مراجعة/نشر
```

## 2. خدمة البريد (SendGrid)

### القوالب
- تأكيد البريد الإلكتروني
- استعادة كلمة المرور
- إشعارات المقالات
- النشرات الإخبارية
- تنبيهات النظام

## 3. API Endpoints
```
POST /api/email-agent/webhook        - استقبال البريد
GET  /api/admin/email-agent/logs     - سجلات البريد
GET  /api/admin/email-agent/settings - الإعدادات
```

---

# 💾 مكتبة الوسائط

## 1. نظرة عامة

نظام إدارة أصول مركزي مع استخراج كلمات مفتاحية بالذكاء الاصطناعي.

## 2. أنواع الملفات المدعومة
| النوع | الامتدادات |
|-------|-----------|
| الصور | jpg, jpeg, png, gif, webp, svg |
| الفيديو | mp4, webm, mov |
| الصوت | mp3, wav, ogg, m4a |
| المستندات | pdf, doc, docx, xls, xlsx |

## 3. الميزات
| الميزة | الوصف |
|--------|-------|
| AI Keywords | استخراج كلمات مفتاحية تلقائي |
| Folder Organization | تنظيم بالمجلدات |
| Search & Filter | بحث وتصفية متقدم |
| Thumbnail Generation | توليد صور مصغرة |
| CDN Integration | تكامل مع CDN |
| Access Control | التحكم بالوصول |

## 4. التخزين
- **المزود:** Google Cloud Storage (Replit Object Storage)
- **المجلدات:**
  - `public/` - أصول عامة
  - `.private/` - ملفات خاصة

## 5. API Endpoints
```
GET    /api/media                    - قائمة الملفات
POST   /api/media/upload             - رفع ملف
GET    /api/media/:id                - تفاصيل ملف
PUT    /api/media/:id                - تحديث ملف
DELETE /api/media/:id                - حذف ملف
GET    /api/media/folders            - قائمة المجلدات
POST   /api/media/folders            - إنشاء مجلد
DELETE /api/media/folders/:id        - حذف مجلد
POST   /api/media/make-public        - جعل الملف عام
GET    /api/media/suggestions        - اقتراحات AI
```

---

# 📊 لوحات التحكم والتحليلات

## 1. لوحة التحكم الرئيسية (`/admin`)

### إحصائيات المحتوى
| الإحصائية | الوصف |
|-----------|-------|
| إجمالي المقالات | عدد المقالات الكلي |
| المقالات المنشورة | المقالات الفعّالة |
| المسودات | مقالات قيد العمل |
| المجدولة | مقالات مجدولة للنشر |

### إحصائيات المستخدمين
| الإحصائية | الوصف |
|-----------|-------|
| إجمالي المستخدمين | عدد المستخدمين الكلي |
| المستخدمون النشطون | نشطون خلال 30 يوم |
| المستخدمون الجدد | مسجلون هذا الشهر |

### إحصائيات خدمات المنصة
| الخدمة | البيانات المعروضة |
|--------|-------------------|
| النشرات الصوتية | الإجمالي، المنشورة، الاستماعات |
| التحليلات العميقة | الإجمالي، المنشورة |
| الناشرون | الإجمالي، النشطون |
| مكتبة الوسائط | عدد الملفات، الحجم الإجمالي |

### إحصائيات الذكاء الاصطناعي
| الخدمة | البيانات المعروضة |
|--------|-------------------|
| مهام AI | الإجمالي، المعلقة، المكتملة |
| صور AI | الإجمالي، هذا الأسبوع |
| القوالب الذكية | الإجمالي النشط |

## 2. لوحة تحليلات المقالات (`/admin/analytics`)

### المقاييس
- المشاهدات اليومية/الأسبوعية/الشهرية
- معدل القراءة
- الوقت على الصفحة
- معدل الارتداد
- المشاركات الاجتماعية
- التفاعلات والتعليقات

### التقارير
- أداء المقالات
- أداء التصنيفات
- أداء الكتّاب
- مصادر الزيارات
- توزيع الأجهزة

## 3. تحليلات التوصيات

### المقاييس
- دقة التوصيات
- معدل النقر
- معدل التحويل
- تفاعل المستخدم

---

# 🏢 نظام الناشرين والوكالات

## 1. نظرة عامة

منصة B2B لنشر المحتوى من وكالات الأنباء والناشرين الخارجيين.

## 2. نظام الرصيد (Credits)

### الباقات
| الباقة | الرصيد | السعر |
|--------|--------|-------|
| Basic | 100 | - |
| Standard | 500 | - |
| Premium | 2000 | - |
| Enterprise | مخصص | - |

### استهلاك الرصيد
- مقال قياسي: 1 رصيد
- مقال مميز: 2 رصيد
- خبر عاجل: 3 رصيد

## 3. صفحات الإدارة

### صفحات الناشر
| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Dashboard | `/publisher` | لوحة تحكم الناشر |
| Articles | `/publisher/articles` | مقالات الناشر |
| Analytics | `/publisher/analytics` | تحليلات الأداء |
| Settings | `/publisher/settings` | إعدادات الحساب |

### صفحات المدير
| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Publishers List | `/admin/publishers` | قائمة الناشرين |
| Publisher Details | `/admin/publishers/:id` | تفاصيل ناشر |
| Articles | `/admin/publishers/:id/articles` | مقالات الناشر |
| Analytics | `/admin/publishers/:id/analytics` | تحليلات الناشر |

## 4. API Endpoints
```
GET    /api/admin/publishers              - قائمة الناشرين
GET    /api/admin/publishers/:id          - تفاصيل ناشر
POST   /api/admin/publishers              - إنشاء ناشر
PATCH  /api/admin/publishers/:id          - تحديث ناشر
DELETE /api/admin/publishers/:id          - حذف ناشر
POST   /api/admin/publishers/:id/credits  - إضافة رصيد
GET    /api/admin/publishers/:id/logs     - سجلات الرصيد
```

---

# 📱 التوافق مع الأجهزة المحمولة

## 1. تطبيقات الهاتف (Capacitor)

### المنصات المدعومة
- iOS (iPhone, iPad)
- Android

### الميزات
| الميزة | الوصف |
|--------|-------|
| Push Notifications | إشعارات فورية |
| Offline Mode | وضع عدم الاتصال |
| Deep Linking | روابط عميقة |
| Splash Screen | شاشة تحميل |
| Status Bar | تخصيص شريط الحالة |
| Keyboard | تحسين لوحة المفاتيح |

## 2. التصميم المتجاوب

### نقاط التوقف
| النقطة | العرض | الاستخدام |
|--------|-------|----------|
| `sm` | 640px | هواتف كبيرة |
| `md` | 768px | أجهزة لوحية |
| `lg` | 1024px | لابتوب |
| `xl` | 1280px | شاشات كبيرة |

### معايير التصميم
- حد أدنى للمس: 24px
- تباعد متجاوب
- أيقونات قابلة للتحجيم
- قوائم قابلة للطي

---

# ♿ إمكانية الوصول (Accessibility)

## 1. معايير WCAG 2.1 AA

### الميزات المطبقة
| الميزة | الوصف |
|--------|-------|
| Skip Links | روابط التخطي |
| ARIA Labels | تسميات ARIA |
| Semantic HTML | HTML دلالي |
| Live Regions | مناطق حية للتحديثات |
| Focus Management | إدارة التركيز |
| Color Contrast | تباين ألوان كافٍ |

## 2. مساعد القراءة

### الميزات
| الميزة | الوصف |
|--------|-------|
| Voice Assistant | مساعد صوتي |
| Reading Mode | وضع القراءة |
| Dyslexia Mode | وضع عسر القراءة |
| Font Scaling | تكبير الخط |
| High Contrast | تباين عالٍ |

## 3. صفحة بيان إمكانية الوصول
**المسار:** `/accessibility`

---

# 🎨 نظام المظاهر والألوان

## 1. المظاهر

### المظاهر المتاحة
- Light Mode (فاتح)
- Dark Mode (داكن)
- Auto (تلقائي حسب النظام)

### إعدادات المظهر
```typescript
themeTokens = {
  colors: Record<string, string>,
  fonts: Record<string, string>,
  spacing: Record<string, string>,
  borderRadius: Record<string, string>
}
```

## 2. أصول العلامة التجارية
```typescript
themeAssets = {
  logoLight: string,   // شعار للوضع الفاتح
  logoDark: string,    // شعار للوضع الداكن
  favicon: string,     // أيقونة الموقع
  banner: string,      // بانر الموقع
  ogImage: string      // صورة المشاركة
}
```

## 3. محرر المظهر (`/admin/theme-editor`)

### الإمكانيات
- تخصيص الألوان
- تغيير الخطوط
- ضبط التباعد
- معاينة مباشرة
- حفظ كمسودة
- نشر التغييرات

---

# 🔔 نظام الإشعارات

## 1. قنوات الإشعارات

| القناة | الوصف | التقنية |
|--------|-------|---------|
| Push | إشعارات فورية | Web Push / FCM |
| Email | بريد إلكتروني | SendGrid |
| SMS | رسائل نصية | Twilio |
| In-App | داخل التطبيق | SSE |
| WhatsApp | واتساب | Twilio |

## 2. أنواع الإشعارات

| النوع | الوصف |
|-------|-------|
| Breaking News | أخبار عاجلة |
| Article Published | مقال جديد منشور |
| Comment Reply | رد على تعليق |
| System Alert | تنبيه نظام |
| Personalized | توصيات مخصصة |

## 3. تفضيلات المستخدم

كل مستخدم يمكنه تخصيص:
- القنوات المفعلة
- أنواع الإشعارات
- أوقات الإزعاج
- تكرار الإشعارات

## 4. API Endpoints
```
GET    /api/notifications              - قائمة الإشعارات
PATCH  /api/notifications/:id/read     - تحديد كمقروء
DELETE /api/notifications/:id          - حذف إشعار
GET    /api/notification-settings      - تفضيلات الإشعارات
PATCH  /api/notification-settings      - تحديث التفضيلات
```

---

# 🔗 نظام الروابط الذكية (Smart Links)

## 1. نظرة عامة

نظام روابط مختصرة مع تتبع وتحليلات.

## 2. الميزات
| الميزة | الوصف |
|--------|-------|
| URL Shortening | اختصار الروابط |
| Click Tracking | تتبع النقرات |
| UTM Parameters | معلمات التتبع |
| QR Codes | رموز QR |
| Expiration | انتهاء الصلاحية |
| Password Protection | حماية بكلمة مرور |

## 3. التحليلات
- عدد النقرات
- مصادر الزيارات
- توزيع الأجهزة
- المواقع الجغرافية
- أوقات الذروة

---

# 📊 القوالب الذكية (Smart Blocks)

## 1. نظرة عامة

نظام قوالب ديناميكية لعرض المحتوى على الصفحة الرئيسية.

## 2. أنواع القوالب

| النوع | الوصف |
|-------|-------|
| Featured | مقالات مميزة |
| Trending | الأكثر رواجًا |
| Category Grid | شبكة تصنيفات |
| Breaking News | أخبار عاجلة |
| Video Carousel | سلايدر فيديو |
| Newsletter CTA | دعوة للاشتراك |
| Ad Space | مساحة إعلانية |
| Custom HTML | HTML مخصص |

## 3. إعدادات القالب
```typescript
smartBlockFilters = {
  categories: string[],   // تصفية بالتصنيفات
  dateRange: {
    from: string,
    to: string
  },
  maxItems: number,
  sortBy: 'date' | 'views' | 'featured',
  refreshInterval: number
}
```

## 4. صفحة الإدارة
**المسار:** `/admin/smart-categories`

---

# 🌍 دعم اللغات المتعددة

## 1. اللغات المدعومة

| اللغة | الكود | الاتجاه |
|-------|-------|---------|
| العربية | `ar` | RTL |
| الإنجليزية | `en` | LTR |
| الأردية | `ur` | RTL |

## 2. صفحات اللغات

### الإنجليزية (`/en`)
- `/en` - الصفحة الرئيسية
- `/en/article/:slug` - صفحة المقال
- `/en/category/:slug` - صفحة التصنيف

### الأردية (`/ur`)
- `/ur` - الصفحة الرئيسية
- `/ur/article/:slug` - صفحة المقال
- `/ur/category/:slug` - صفحة التصنيف

## 3. إدارة الترجمات
- ترجمة واجهة المستخدم
- ترجمة المحتوى
- ملفات اللغة المستقلة
- تبديل اللغة الديناميكي

---

# 🎫 بطاقات Apple Wallet

## 1. أنواع البطاقات

### بطاقة الصحفي (Press Card)
| الحقل | الوصف |
|-------|-------|
| الاسم | اسم الصحفي |
| المسمى | المسمى الوظيفي |
| الصورة | صورة شخصية |
| رقم البطاقة | رقم فريد |
| تاريخ الانتهاء | صلاحية البطاقة |
| QR Code | رمز التحقق |

### بطاقة الولاء (Loyalty Card)
| الحقل | الوصف |
|-------|-------|
| الاسم | اسم العضو |
| الرتبة | مستوى العضوية |
| النقاط | رصيد النقاط |
| رقم العضوية | رقم فريد |
| QR Code | رمز العضوية |

## 2. تكامل PassKit
- توليد تلقائي للبطاقات
- تحديث فوري
- إشعارات البطاقة
- دعم iOS و watchOS

---

# 🔧 الوظائف المجدولة (Jobs)

## 1. قائمة الوظائف

| الوظيفة | الملف | الجدول | الوصف |
|---------|-------|--------|-------|
| AI Tasks Executor | `aiTasksJob.ts` | كل دقيقة | تنفيذ مهام AI |
| AI Tasks Cleanup | `aiTasksCleanup.ts` | كل 5 دقائق | تنظيف مهام عالقة |
| iFox Generator | `ifoxContentGeneratorJob.ts` | كل دقيقة | توليد محتوى AI |
| Audio Newsletter | `audioNewsletterJob.ts` | حسب الجدول | نشر النشرات الصوتية |
| Dynamic Categories | `dynamicCategoriesJob.ts` | كل ساعة | تحديث تصنيف "الآن" |
| Seasonal Categories | `seasonalCategoriesJob.ts` | كل 6 ساعات | تصنيفات موسمية |
| Campaign Reset | `campaignDailyResetJob.ts` | منتصف الليل | إعادة تعيين الحملات |
| Calendar | `calendar.ts` | حسب الإعداد | مهام التقويم |

## 2. Newsletter Scheduler

### الجداول المعدة
| النشرة | التوقيت | الوصف |
|--------|---------|-------|
| Morning Brief | 6:00 صباحاً | نشرة صباحية |
| Evening Digest | 8:00 مساءً | ملخص مسائي |
| Weekly Roundup | الأحد 10:00 صباحاً | جولة أسبوعية |

---

# 🛠️ البنية التقنية

## 1. Frontend Stack

| التقنية | الإصدار | الاستخدام |
|---------|---------|----------|
| React | 18 | مكتبة UI |
| TypeScript | 5.x | لغة البرمجة |
| Vite | 5.x | أداة البناء |
| TanStack Query | 5.x | إدارة الحالة والـ Cache |
| Wouter | 3.x | التوجيه |
| Tailwind CSS | 4.x | التنسيق |
| Radix UI | - | مكونات UI |
| Framer Motion | - | الحركات |

## 2. Backend Stack

| التقنية | الإصدار | الاستخدام |
|---------|---------|----------|
| Node.js | 20.x | بيئة التشغيل |
| Express.js | 4.x | إطار الخادم |
| TypeScript | 5.x | لغة البرمجة |
| Drizzle ORM | - | إدارة قاعدة البيانات |
| Passport.js | - | المصادقة |
| Zod | - | التحقق من البيانات |

## 3. قاعدة البيانات

| التقنية | الاستخدام |
|---------|----------|
| PostgreSQL | قاعدة البيانات الرئيسية |
| Neon Serverless | مزود الخدمة |
| Drizzle ORM | ORM |

## 4. خدمات خارجية

| الخدمة | الاستخدام |
|--------|----------|
| OpenAI (GPT-5.1) | الذكاء الاصطناعي |
| ElevenLabs | تحويل النص لكلام |
| Twilio | SMS و WhatsApp |
| SendGrid | البريد الإلكتروني |
| Google Cloud Storage | تخزين الملفات |
| Google OAuth | تسجيل دخول جوجل |
| Apple OAuth | تسجيل دخول آبل |

## 5. هيكل الملفات

```
├── client/                    # Frontend
│   └── src/
│       ├── components/        # مكونات React
│       ├── pages/            # صفحات التطبيق
│       │   ├── admin/        # صفحات الإدارة
│       │   ├── ai/           # صفحات AI
│       │   ├── ifox/         # صفحات iFox
│       │   ├── en/           # صفحات إنجليزية
│       │   └── ur/           # صفحات أردية
│       ├── hooks/            # React Hooks
│       ├── lib/              # أدوات مساعدة
│       └── contexts/         # React Contexts
├── server/                    # Backend
│   ├── services/             # خدمات الأعمال
│   ├── routes/               # مسارات API
│   ├── jobs/                 # وظائف مجدولة
│   ├── ai/                   # خدمات AI
│   └── utils/                # أدوات مساعدة
├── shared/                    # كود مشترك
│   └── schema.ts             # نماذج البيانات
└── attached_assets/          # الأصول المرفقة
```

---

# 📈 API Reference

## 1. المصادقة
```
POST   /api/login
POST   /api/register
POST   /api/logout
GET    /api/auth/user
GET    /api/auth/google
GET    /api/auth/apple
POST   /api/auth/verify-email
POST   /api/auth/forgot-password
POST   /api/auth/reset-password
```

## 2. المستخدمين
```
GET    /api/users
GET    /api/users/:id
GET    /api/admin/users
POST   /api/admin/users
PATCH  /api/admin/users/:id
DELETE /api/admin/users/:id
PATCH  /api/admin/users/:id/roles
```

## 3. المقالات
```
GET    /api/articles
GET    /api/articles/:slug
GET    /api/admin/articles
POST   /api/admin/articles
PATCH  /api/admin/articles/:id
DELETE /api/admin/articles/:id
POST   /api/admin/articles/:id/publish
```

## 4. التصنيفات
```
GET    /api/categories
GET    /api/categories/:slug
GET    /api/admin/categories
POST   /api/admin/categories
PATCH  /api/admin/categories/:id
DELETE /api/admin/categories/:id
```

## 5. الوسائط
```
GET    /api/media
POST   /api/media/upload
GET    /api/media/:id
PUT    /api/media/:id
DELETE /api/media/:id
```

## 6. النشرات الصوتية
```
GET    /api/audio-newsletters
GET    /api/audio-newsletters/:id
POST   /api/admin/audio-newsletters
PATCH  /api/admin/audio-newsletters/:id
DELETE /api/admin/audio-newsletters/:id
```

## 7. iFox
```
GET    /api/ifox/articles
GET    /api/ifox/categories
GET    /api/admin/ifox/articles
POST   /api/admin/ifox/articles
PATCH  /api/admin/ifox/articles/:id
DELETE /api/admin/ifox/articles/:id
```

## 8. مهام AI
```
GET    /api/admin/ai-tasks
POST   /api/admin/ai-tasks
PATCH  /api/admin/ai-tasks/:id
DELETE /api/admin/ai-tasks/:id
POST   /api/admin/ai-tasks/:id/retry
```

---

# 🔒 الأمان

## 1. ممارسات الأمان

| الممارسة | التطبيق |
|----------|---------|
| Password Hashing | bcrypt |
| Session Management | express-session + PostgreSQL |
| CSRF Protection | CSRF tokens |
| Rate Limiting | express-rate-limit |
| Input Validation | Zod schemas |
| SQL Injection | Drizzle ORM (parameterized) |
| XSS Prevention | DOMPurify |
| CORS | cors middleware |
| Helmet | HTTP headers security |
| HTTPS | TLS/SSL |

## 2. صلاحيات الـ RBAC

نظام صلاحيات متدرج يضمن:
- فصل الصلاحيات
- مبدأ الحد الأدنى
- تدقيق الإجراءات

---

# 📝 متغيرات البيئة

## 1. المتغيرات المطلوبة

```env
# قاعدة البيانات
DATABASE_URL=postgresql://...

# المصادقة
SESSION_SECRET=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
APPLE_CLIENT_ID=...
APPLE_TEAM_ID=...
APPLE_KEY_ID=...
APPLE_PRIVATE_KEY=...

# AI Services
OPENAI_API_KEY=...
ELEVENLABS_API_KEY=...

# الاتصالات
TWILIO_ACCOUNT_SID=...
TWILIO_AUTH_TOKEN=...
TWILIO_PHONE_NUMBER=...
SENDGRID_API_KEY=...

# التخزين
GCS_BUCKET_NAME=...
GCS_PROJECT_ID=...
```

---

# 📞 الدعم والتواصل

## للمساعدة التقنية
- مراجعة هذه الوثائق
- فحص سجلات النظام
- التواصل مع فريق التطوير

---

> **ملاحظة:** هذه الوثائق قابلة للتحديث مع تطور النظام. آخر تحديث: نوفمبر 2025
