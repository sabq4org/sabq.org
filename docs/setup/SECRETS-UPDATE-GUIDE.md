# 🔑 دليل تحديث Secrets

## المشكلة الحالية

المتغيرات البيئية لا تزال تشير إلى bucket قديم:
```
❌ PUBLIC_OBJECT_SEARCH_PATHS=/replit-objstore-b1f39c51-f362-497c-846b-74ce14cc0e52/public
❌ PRIVATE_OBJECT_DIR=/replit-objstore-b1f39c51-f362-497c-846b-74ce14cc0e52/.private
```

---

## ✅ الخطوات الصحيحة

### 1. افتح Secrets
- في القائمة اليسرى، ابحث عن: **🔒 Secrets**
- أو من **Tools** → **Secrets**

### 2. تحديث `PUBLIC_OBJECT_SEARCH_PATHS`

#### أ) ابحث عن المتغير:
- اكتب `PUBLIC_OBJECT_SEARCH_PATHS` في مربع البحث
- أو scroll للأسفل حتى تجده

#### ب) احذف القيمة القديمة:
- اضغط على المتغير لفتحه
- **امسح** القيمة القديمة **كاملة**
- يجب أن يصبح الحقل فارغاً تماماً

#### ج) أدخل القيمة الجديدة:
```
/replit-objstore-3dc2325c-bbbe-4e54-9a00-e6f10b243138/public
```
- **لا تضيف مسافات** قبل أو بعد
- **لا تضيف علامات اقتباس** (" أو ')

#### د) احفظ:
- اضغط **Save** أو **Update**
- انتظر رسالة التأكيد

---

### 3. تحديث `PRIVATE_OBJECT_DIR`

#### أ) ابحث عن المتغير:
- اكتب `PRIVATE_OBJECT_DIR` في مربع البحث

#### ب) احذف القيمة القديمة:
- اضغط على المتغير
- امسح القيمة القديمة كاملة

#### ج) أدخل القيمة الجديدة:
```
/replit-objstore-3dc2325c-bbbe-4e54-9a00-e6f10b243138/.private
```
- **لاحظ النقطة** قبل `private` (مهمة جداً)
- **لا تضيف مسافات** قبل أو بعد

#### د) احفظ:
- اضغط **Save** أو **Update**

---

### 4. إعادة تشغيل السيرفر

#### الطريقة الأولى (تلقائي):
- السيرفر يعيد التشغيل **تلقائياً** بعد تغيير Secrets
- انتظر 10-15 ثانية

#### الطريقة الثانية (يدوي):
- اضغط زر **Stop** (في أعلى الشاشة)
- ثم اضغط **Run**

---

## ✅ التحقق من النجاح

بعد إعادة التشغيل، يجب أن ترى في Console/Logs:

```
✅ السيرفر بدأ بنجاح
✅ لا توجد أخطاء 401 أو 500
```

---

## 🧪 الاختبار

1. سجّل دخول كمدير
2. اذهب إلى **إنشاء مقال جديد**
3. حاول رفع **صورة رئيسية**
4. يجب أن يتم الرفع **بنجاح** ✅

---

## ⚠️ ملاحظات مهمة

### إذا لم تتحدث القيم:

1. **تأكد من الحفظ:**
   - يجب الضغط على **Save/Update** بعد كل تغيير
   
2. **تأكد من عدم وجود مسافات:**
   ```
   ✅ /sabq-production-bucket/public
   ❌  /sabq-production-bucket/public  (مسافة في البداية/النهاية)
   ```

3. **تأكد من الإملاء:**
   - `replit-objstore-3dc2325c-bbbe-4e54-9a00-e6f10b243138` (نسخ كامل)
   - `.private` (بنقطة في البداية)

4. **انتظر قليلاً:**
   - بعد الحفظ، انتظر 5-10 ثوانٍ
   - ثم جرّب رفع الصورة

---

## 📞 إذا استمرت المشكلة

إذا بعد تنفيذ كل الخطوات أعلاه لا تزال تحصل على خطأ 500:

1. التقط screenshot من صفحة Secrets
2. التقط screenshot من خطأ 500
3. أخبرني لأساعدك في استكشاف المشكلة
