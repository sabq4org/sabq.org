# نظام iFox (`ifox`)

> آخر مراجعة: 2026-09-05 | المالك: ai

## الغرض
منصة توليد وإدارة محتوى بالذكاء الاصطناعي: مقالات، تقويم، ميزانية، جودة، وسائط، ومهام AI المجدولة.

## مفاتيح AI (حصرية)
`ifox-content`, `ifox-quality`, `ifox-strategy`, `ai-task-executor`

## نقاط الدخول
`server/services/ifox/**`, `server/routes/ifox/**`, `aiTaskExecutor`, صفحات `/dashboard/admin/ifox/*`

## عقود مهمة
- الميزانية والجودة جزء من دورة النشر.
- سجّل الاستهلاك عبر Gateway بمفاتيح ifox الواضحة.

## عند التعديل
- [ ] قرأت هذا الملف
- [ ] أي نموذج جديد يمر Gateway ويُسجَّل usage

## سلامة التوليد المجدول — مراجعة 2026-09-05
- المطالبة planned → in_progress ذرية، وupdatedAt رمز ملكية المحاولة.
- إنشاء المقال وربطه بالمهمة في transaction واحدة؛ فشل الربط يرجع إنشاء المقال أيضًا. العامل القديم لا يكمل أو يعيد جدولة مهمة غيّرها خليفته.
- مهمة in_progress المتوقفة 30 دقيقة تصبح failed للمراجعة، بدل توليد نسخة ثانية تلقائيًا.
- قواعد الجودة والاعتماد والنشر الحالية محفوظة؛ لم تُفعّل أي مهمة على الإنتاج.
