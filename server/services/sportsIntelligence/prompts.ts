/**
 * بناء المطالبات بالتأريض الصارم: نحقن الوقائع فقط، ونمنع النموذج صراحةً من
 * اختلاق أي معلومة غير معطاة — نفس قاعدة generateMatchStory في saudiLeagueService.
 * كل مُطلِّق يُرجِع نصّ المطالبة؛ التوليد نفسه في aiClient.
 */

const GROUNDING = `أنت محلّل رياضي في صحيفة «سبق». التزم حصرياً بالوقائع والأرقام المعطاة أدناه، ولا تختلق أي معلومة غير مذكورة (لا أسماء، لا أرقام، لا أحداث). العربية الفصحى، بلا تحيّز، وبلا رموز أو إيموجي.`;

/** لقطات «المشهد الآن»: تحليل دفعة من المباريات البارزة → بطاقات JSON. */
export function sceneCardsPrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: اقرأ قائمة المباريات البارزة (جارية/انتهت اليوم) وأنتج «التقاطات» موجزة — لكل مباراة ذات دلالة بطاقةٌ من جملة إلى جملتين تلتقط أهم ما فيها (نتيجة مفاجئة، تقارب، تألّق، منعطف). لا تكرّر ما هو بديهي من مجرّد النتيجة.

أعِد JSON فقط بهذا الشكل دون أي نص خارجه:
{"cards":[{"fixtureId":<رقم>,"headline":"<عنوان قصير ≤ 8 كلمات>","body":"<جملة أو جملتان>"}],"summary":"<جملة واحدة تلخّص مزاج المشهد الرياضي الآن>"}

الوقائع:
${factsBlock}`;
}

/** بطاقة المباراة الذكية قبل الانطلاق. */
export function matchPrePrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: بطاقة تشويقية لمباراة مرتقبة. عنوان قصير + جملتان تمهيديتان + حتى ثلاث نقاط «ما يجب أن تعرفه» مبنية حصراً على الوقائع (سجل المواجهات، الترجيحات، السياق).

أعِد JSON فقط:
{"headline":"<≤ 8 كلمات>","body":"<جملتان>","bullets":["<نقطة>","<نقطة>"]}

الوقائع:
${factsBlock}`;
}

/** بطاقة المباراة الذكية أثناء اللعب. */
export function matchLivePrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: قراءة لحظية للمباراة الجارية بصيغة الحاضر — عنوان يلتقط الوضع الآن + جملتان + حتى ثلاث نقاط عن مفاتيح اللحظة (استحواذ، ضغط، منعطف، هدّاف) من الوقائع فقط.

أعِد JSON فقط:
{"headline":"<≤ 8 كلمات>","body":"<جملتان>","bullets":["<نقطة>","<نقطة>"]}

الوقائع:
${factsBlock}`;
}

/** بطاقة المباراة الذكية بعد النهاية. */
export function matchPostPrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: خلاصة ما بعد المباراة بصيغة الماضي — عنوان يلخّص النتيجة ودلالتها + جملتان + حتى ثلاث نقاط عن العوامل الحاسمة (من الأحداث والإحصاءات) من الوقائع فقط.

أعِد JSON فقط:
{"headline":"<≤ 8 كلمات>","body":"<جملتان>","bullets":["<نقطة>","<نقطة>"]}

الوقائع:
${factsBlock}`;
}

/** قصص الموسم/الأنماط من إشارات رقمية مُحسوبة مسبقاً. */
export function trendsPrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: حوّل الإشارات الرقمية المحسوبة أدناه إلى «قصص موسم» موجزة — كل إشارة ذات دلالة تصير بطاقة (عنوان + جملة أو جملتان تفسّر الرقم وتضعه في سياقه). لا تضف أرقاماً غير معطاة.

أعِد JSON فقط:
{"cards":[{"kind":"trend|anomaly","importance":<0-100>,"headline":"<≤ 8 كلمات>","body":"<جملة أو جملتان>"}]}

الإشارات:
${factsBlock}`;
}

/** تعليل بشري لتوقّع مباراة من ترجيحات المزوّد + السياق. */
export function predictionPrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: حوّل الترجيحات الرقمية والسياق إلى تعليل بشري موجز — لماذا يميل الترجيح لهذا الطرف؟ عنوان + جملتان. اذكر أن الأمر ترجيح لا حتمية.

أعِد JSON فقط:
{"headline":"<≤ 8 كلمات>","body":"<جملتان>"}

الوقائع:
${factsBlock}`;
}

/** الموجز الرياضي المخصّص من متابعات المستخدم. */
export function digestPrompt(factsBlock: string): string {
  return `${GROUNDING}

المطلوب: اكتب موجزاً رياضياً شخصياً موجّهاً لمتابعٍ يهتمّ بالفِرق/البطولات أدناه — ماذا فاته وماذا ينتظره. عنوان ودّي + فقرة قصيرة (٦٠-١٠٠ كلمة) نصّاً متّصلاً.

أعِد JSON فقط:
{"headline":"<≤ 9 كلمات>","body":"<فقرة>"}

الوقائع:
${factsBlock}`;
}

/** المساعد المحادثي (RAG): يجيب من السياق المُحقون فقط. */
export function copilotPrompt(question: string, contextBlock: string): string {
  return `${GROUNDING}

أجب عن سؤال المستخدم اعتماداً حصرياً على «السياق» أدناه (بيانات حيّة من بطولاتنا). إن لم يكفِ السياق للإجابة، قل بوضوح إن المعلومة غير متوفّرة لديك الآن ولا تخمّن. أجب بإيجاز (جملتان إلى أربع) بالعربية.

السؤال: ${question}

السياق:
${contextBlock}`;
}
