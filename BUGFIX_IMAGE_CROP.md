# 🐛 إصلاح: اقتطاع الصور من الأسفل

## التاريخ
2026-05-28

## المشكلة
صور بطاقات الأخبار كانت تظهر مقتطعة من الأسفل في الصفحة الرئيسية.

## السبب الجذري
في ملف `/client/src/lib/imageUtils.ts`، دالة `getObjectPosition` كانت تستخدم:

```typescript
// قبل الإصلاح
defaultPosition: string = 'center 20%'
```

`object-position: center 20%` تعني: "اعرض الجزء الذي يقع على ارتفاع 20% من أعلى الصورة كمركز".  
هذا يسحب الصورة نحو الأعلى، ويقطع الجزء السفلي عند استخدام `object-fit: cover` مع حاوية ذات `aspect-ratio` محدد.

## الإصلاح
```typescript
// بعد الإصلاح
defaultPosition: string = 'center center'
```

`center center` يعرض مركز الصورة الحقيقي ويوزع الاقتطاع بالتساوي من جميع الجهات.

## الملفات المتأثرة
- `/client/src/lib/imageUtils.ts` — السطر 102
- تؤثر على جميع بطاقات الأخبار التي تستدعي `getObjectPosition()`:
  - `ArticleCard.tsx`
  - `NewsArticleCard.tsx`
  - `BaseArticleCard.tsx`
  - وغيرها

## ملاحظة
إذا كانت الصورة تحتوي على `imageFocalPoint` أو `image_focal_point` (بيانات نقطة التركيز)، يُستخدم الموضع المحدد فيها تلقائياً بدل القيمة الافتراضية.  
القيمة الافتراضية `center center` تُطبق فقط على الصور التي لا تحمل بيانات focal point.

## الاختبار
تحقق بصرياً من بطاقات الأخبار في:
- الصفحة الرئيسية
- صفحات الأقسام
- بطاقات "أكثر قراءة"

## خلاصة
**قبل:** `center 20%` — يعرض الجزء العلوي ويقطع الأسفل  
**بعد:** `center center` — يعرض المنتصف، اقتطاع متوازن من الجهتين
