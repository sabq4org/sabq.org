node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets — NEVER commit
.env
.env.local
.env.*.local
.env.railway
.env.vercel

# Replit-specific config
.replit
replit.nix

# Seed data with personal info — kept local only
scripts/seed-data/

# Build artifacts
.cache
*.log
.turbo

# Android/iOS build outputs
android/app/build
android/.gradle
ios/App/App/public
ios/build

# Temporary files
*.tmp
*.temp
backup_unused_assets

# Database backups and SQL scripts (NEVER commit to repo)
*.sql
!migrations/*.sql
*.sql.gz
*.dump
*.backup

# Debug screenshots
*.png
!client/public/**/*.png
!client/src/**/*.png
!public/**/*.png
!android/**/*.png
!android-native/**/*.png
!ios/**/*.png
!attached_assets/**/*.png
# Apple Wallet pass templates need their icon/logo PNGs committed —
# the runtime container reads them from disk when generating .pkpass.
!server/lib/passkit/**/*.png
# Internal security audit — kept local; contains attack-surface details
AUDIT_REPORT.md

# Internal migration runbook — kept local; contains operational detail
MIGRATION_RUNBOOK.md

# Auto-generated middleware embed (regenerated each build)
spa-index-html.generated.ts
*.p12
sabq-press.p12
